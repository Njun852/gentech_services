<UserControl x:Class="gentech_services.Views.Pages.POSPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:gentech_services.Views.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="1000" d:DesignWidth="850"
             xmlns:userControl="clr-namespace:gentech_services.Views.UserControls">

    <Border Grid.Column="1" Background="white" MaxWidth="1000" MinWidth="800">
        <Grid Background="#FFFFFF"  >
            <Grid.RowDefinitions>
                <RowDefinition Height="5*"/>
                <RowDefinition Height="45*"/>
                <RowDefinition Height="50*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Point of Sale" Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="40" FontWeight="SemiBold"  />
            <Border Grid.Row="1" BorderBrush="#000000" CornerRadius="12.75" BorderThickness="1" Padding="20" Margin="0,20,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Grid.ColumnSpan="3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="276"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <userControl:Searchbar Grid.Column="0" Loaded="Searchbar_Loaded"/>
                        <ComboBox Grid.Column="2" Style="{StaticResource OutlineComboBox}" Width="160" Margin="0,0,8,0"   Tag="All Categories" Height="30"/>
                    </Grid>
                    <ScrollViewer Margin=" 0,10,0,0"  Grid.Row="1" Grid.ColumnSpan="3" Style="{StaticResource CustomScrollViewer}">
                        <ItemsControl
                                        ItemsSource="{Binding ServiceOrders, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        d:ItemsSource="{d:SampleData ItemCount=5}"
                                        AlternationCount="2">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <userControl:Card TextName="AMD Ryzen 5 5600" TextCategory="Processor" TextPrice="8950" TextStock="4"/>

                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                    </ScrollViewer>
                </Grid>

            </Border>
            <Border Grid.Row="2" Padding="20"  BorderBrush="#000000" CornerRadius="12.75" BorderThickness="1" Margin="0,20,0,0" >
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="60*"/>
                        <ColumnDefinition Width="40*"/>
                    </Grid.ColumnDefinitions>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Sales Cart" Foreground="#0A0A0A" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
                        <userControl:POSTable Grid.Row="1"/>
                    </Grid>
                    <StackPanel Orientation="Vertical" Grid.Column="1" Margin="20,0,0,0">
                        <Grid >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Total " FontSize="18" Grid.Column="0" TextAlignment="Center" VerticalAlignment="Bottom" />
                            <TextBlock Text="Total " FontSize="24" Grid.Column="2" FontWeight="Bold" />
                        </Grid>

                        <Button Content="Process Payment" FontSize="14" FontWeight="Medium" Tag="3" Background="#0000FF" Foreground="White" Padding="5"  Style="{StaticResource OutlineButton}" Margin="0,20,0,0" />
                        <Button  Content="Clear" FontSize="14" Tag="3"  FontWeight="Medium" Background="#FFFFFF" Foreground="#000000" Padding="5" Style="{StaticResource OutlineButton}" Margin="0,10,0,0" />

                        <TextBlock Text="Customer Information" FontSize="16" FontWeight="Bold" Margin="0,20,0,10"/>
                        <TextBlock Text="First Name" FontSize="12" FontWeight="Bold" Margin="0,0,0,3"/>
                        <TextBox Tag="First Name" FontSize="14" Background="#F3F3F5" Style="{StaticResource OutlineTextBox}" Margin="0,0,0,10"/>
                        <TextBlock Text="Last Name" FontSize="12" FontWeight="Bold" Margin="0,0,0,3"/>
                        <TextBox Tag="Last Name" FontSize="14" Background="#F3F3F5" Style="{StaticResource OutlineTextBox}" Margin="0,0,0,10"/>
                        <TextBlock Text="Email" FontSize="12" FontWeight="Bold" Margin="0,0,0,3"/>
                        <TextBox Tag="Email" FontSize="14" Background="#F3F3F5" Style="{StaticResource OutlineTextBox}" Margin="0,0,0,10"/>
                        <TextBlock Text="Phone Number" FontSize="12" FontWeight="Bold" Margin="0,0,0,3"/>
                        <TextBox Tag="Phone Number" FontSize="14" Background="#F3F3F5" Style="{StaticResource OutlineTextBox}" Margin="0,0,0,10"/>
                    </StackPanel>
                </Grid>
            </Border>

        </Grid>

    </Border>
</UserControl>
