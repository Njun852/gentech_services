<UserControl x:Class="gentech_services.Views.UserControls.LowStockAlert"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:gentech_services.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="800">
    <Border Background="#1AD4183D" BorderBrush="#33D4183D" BorderThickness="1" MinHeight="80" CornerRadius="8.75" Padding="15">
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Image Source="pack://application:,,,/gentech_services;component/Resources/Assets/Icons/icnWarning.png" Width="16" Height="16" Margin="0,0,10,0"/>
                <TextBlock Text="Low Stock Alert" Foreground="#D4183D" FontSize="12"/>
            </StackPanel>
                <ItemsControl
                    ItemsSource="{Binding ServiceOrders, RelativeSource={RelativeSource AncestorType=UserControl}}"
        d:ItemsSource="{d:SampleData ItemCount=5}"
        AlternationCount="2">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                        <Border Background="#D4183D" Height="19.5" MinWidth="120" CornerRadius="6.75" Padding="8,2" Margin="0,0,10,10">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" >
                                <TextBlock Name="lblProductName"  Text="GTX 3050" FontSize="10" Foreground="White"/>
                                <TextBlock Name="lblStock" Text="{Binding TextStock, ElementName=root,  StringFormat={}({0} left)}" FontSize="10" Margin="5,0,0,0" Foreground="White"/>

                            </StackPanel>
                        </Border>

                    </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
        </StackPanel>
    </Border>
    
</UserControl>
