<UserControl x:Class="gentech_services.Views.UserControls.Sidebar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:gentech_services.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="1000" 
             d:DesignWidth="400">

    <UserControl.Resources>
        <Style x:Key="SidebarButtonStyle" TargetType="local:IconButton">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>

            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontFamily" Value="Inter"/>
            <Setter Property="BorderThickness" Value="0"/>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#292929"/>
                    <Setter Property="Foreground" Value="#0000FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="SidebarSubButtonStyle" TargetType="local:IconButton">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="50,0,0,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Inter"/>
            <Setter Property="BorderThickness" Value="0"/>


            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#292929"/>
                    <Setter Property="Foreground" Value="#0000FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </UserControl.Resources>

    <DockPanel Background="Black" >

        <Image DockPanel.Dock="Top"  Stretch="Fill" Width="400px" Height="350px" Source="/Resources/Assets/Icons/logo.png"></Image>
        <StackPanel Orientation="Vertical"  DockPanel.Dock="Top">
            <local:IconButton  Text="Profile" Icon="/Resources/Assets/Icons/account_circle.png" Click="Profile_Click" Style="{StaticResource SidebarButtonStyle}" Loaded="IconButton_Loaded_2"/>
            <local:IconButton  Text="Dashboard" Icon="/Resources/Assets/Icons/dashboard_customize.png"  Click="Dashboard_Click"  Style="{StaticResource SidebarButtonStyle}"/>
            <local:IconButton  Text="Services" Icon="/Resources/Assets/Icons/home_repair_service.png"  Click="Services_Click"  Style="{StaticResource SidebarButtonStyle}" Loaded="IconButton_Loaded_1"/>
            <StackPanel x:Name="ServiceSubButtons" Orientation="Vertical" HorizontalAlignment="Stretch" Visibility="Visible">
                <local:IconButton  Text="Service Management" Click="ServiceManagement_Click" Style="{StaticResource SidebarSubButtonStyle}"/>
                <local:IconButton  Text="Service Orders"  Click="ServiceOrders_Click"  Style="{StaticResource SidebarSubButtonStyle}"/>
            </StackPanel>
            <local:IconButton  Text="Products" Icon="/Resources/Assets/Icons/Product.png"  Click="Products_Click"  Style="{StaticResource SidebarButtonStyle}" Loaded="IconButton_Loaded_1"/>
            <StackPanel x:Name="ProductsSubButtons" Orientation="Vertical" HorizontalAlignment="Stretch" Visibility="Visible">
                <local:IconButton  Text="Inventory Management" Click="InventoryManagement_Click" Style="{StaticResource SidebarSubButtonStyle}"/>
                <local:IconButton  Text="Product Orders"  Click="ProductOrders_Click"  Style="{StaticResource SidebarSubButtonStyle}"/>
            </StackPanel>
            <local:IconButton  Text="Users" Icon="/Resources/Assets/Icons/Product.png"  Click="Users_Click"  Style="{StaticResource SidebarButtonStyle}" Loaded="IconButton_Loaded_1"/>
        </StackPanel>

        <StackPanel Orientation="Vertical" DockPanel.Dock="Bottom" VerticalAlignment="Bottom" Margin="0,0,0,50" >
            <local:IconButton  Text="Logout" Icon="/Resources/Assets/Icons/account_circle.png" Click="Logout_Click" Style="{StaticResource SidebarButtonStyle}" Loaded="IconButton_Loaded_1"/>
            <Grid HorizontalAlignment="Left" Margin="20,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Width="32px" Height="32px" Margin="0,0,8,0" Background="White" CornerRadius="100" BorderThickness="1" BorderBrush="White">
                    <Ellipse>
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="/Resources/Assets/Images/profile image.png"/>
                        </Ellipse.Fill>
                    </Ellipse>
                </Border>

                <StackPanel Grid.Column="1" Margin="4,0,0,0" Orientation="Vertical">
                    <TextBlock Text="Nicole Juntilla" Foreground="white" FontSize="16" Margin="0,0,0,5"/>
                    <TextBlock Text="Technician" Foreground="white" FontSize="10"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </DockPanel>


</UserControl>
