<UserControl x:Class="gentech_services.Views.UserControls.Searchbar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:gentech_services.Views.UserControls"
             mc:Ignorable="d" 
             >
    
    <Border Padding="5,5" Background="#F3F3F5" Width="276" Height="32" CornerRadius="6.75">
        <Grid >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Image Grid.Column="0" Source="/Resources/Assets/Icons/icnSearch.png"  Width="14" Height="14"
                VerticalAlignment="Center"
                HorizontalAlignment="Left"
                IsHitTestVisible="False"/>
            <Grid Grid.Column="1">
                <TextBox  Background="#F3F3F5" Name="txtSearch" Padding="30,0,5,0" BorderThickness="0"/>
                <TextBlock Text="Search by name, category, or supplier..." Foreground="#717182" IsHitTestVisible="False" FontSize="12">

                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=Searchbar}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
            
        </Grid>
        
    </Border>
   
</UserControl>
